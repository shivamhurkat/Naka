import PageContainer from "@/components/PageContainer";

export default function LoginPage() {
  return (
    <PageContainer className="flex flex-col items-center justify-center min-h-screen">
      <h1 className="text-2xl font-bold text-neutral-800">Login</h1>
      <p className="mt-2 text-neutral-500">Sign-in page — coming in step 2.</p>
    </PageContainer>
  );
}
